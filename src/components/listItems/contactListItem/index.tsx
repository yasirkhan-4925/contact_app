import {AppText} from '@app/components';
import React from 'react';
import {View} from 'react-native';

const ContactListItem = () => {
  return (
    <View>
      <AppText>Contact List Item</AppText>
    </View>
  );
};

export default ContactListItem;

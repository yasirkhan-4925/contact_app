import {AppText} from '@app/components';
import React from 'react';
import {View} from 'react-native';

const SearchBar = () => {
  return (
    <View>
      <AppText>Search bar here</AppText>
    </View>
  );
};

export default SearchBar;

import {AppText} from '@app/components';
import React from 'react';
import {View} from 'react-native';

const Button = () => {
  return (
    <View>
      <AppText>This is custom button</AppText>
    </View>
  );
};

export default Button;
